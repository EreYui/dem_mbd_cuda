# Changelog

## Per-body motion state — 2026-08-10

- 为 `BODY` 增加由 `CouplePolyFileName.csv` 末列读取的 `state`。
- 支持 `0`（载荷驱动）、`1`（固定位姿）和 `2`（按 CSV 初始线/角速度作规定运动）。
- 保持旧 24 列刚体 CSV 兼容，缺省状态为 `0`；非法状态会终止并报告具体输入文件。
- 固定体和规定运动体继续参与 CUDA 三角面接触并输出接触载荷，但不会被载荷改变运动状态。
- 新增两刚体状态回归测试，验证固定体锁定、规定平移及规定转动。

## CUDA migration — 2026-08-08

- 将正式构建改为 C++17/CUDA 17，默认本机 GPU 架构，且必须存在可用 NVIDIA CUDA 设备。
- 引入持久化 GPU SoA，迁移颗粒网格、动态三角形网格、邻域搜索、完整接触模型、刚体载荷归约与颗粒 leapfrog 积分。
- 保留通用 `multibody.cpp`/`dyneq.cpp` MBD 方程，刚体和三角形数量由输入动态确定。
- 接触历史改用稳定颗粒 ID 对，以及颗粒 ID/刚体 ID/局部三角形 ID；加入容量、高水位和溢出诊断。
- 颗粒—颗粒、颗粒—刚体、颗粒—墙面继续使用各自参数路由，保持双精度且未启用快速数学近似。
- 输出改为 `std::thread` 双缓冲；只在输出步下载已启用的数据。
- Windows 和 Ubuntu 构建脚本加入工具链、GPU、CTest 与设备诊断检查。
- 新增自由落体、墙面、两颗粒、三角面、纯 DEM 和通用 DEM–MBD CUDA 测试，以及输出误差/守恒量诊断工具。
- 改进跨平台算例路径解析和输入错误报告。
- 保持 `DATA`、`DATA1`、绝对/相对算例目录和参数文件启动形式。
